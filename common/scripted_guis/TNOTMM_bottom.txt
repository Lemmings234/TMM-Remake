scripted_gui = {
	TNOTMM_QIN_LegYuan_bottom_gui = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "TNOTMM_QIN_LegYuan_bottom_gui"
		
		visible = {
			has_country_flag = TMM_QIN_country_has_gui
		}
		
		triggers = {
			china_gui_crt_icon_visible = {
				original_tag = QIN
			}
		}
		effects = {
			china_gui_crt_icon_click = {
				if = {
					limit = { has_country_flag = open_asia_parliament_gui }
					clr_country_flag = open_asia_parliament_gui
					QINGenerateSupport = yes
				}
				else = { set_country_flag = open_asia_parliament_gui }
			}
		}
	}
	TNOTMM_arrows_alert = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "TNOTMM_arrows_alert"

		visible = {
			has_country_flag = TMM_QIN_country_has_gui
		}

		triggers = {
			TNOTMM_gui_alert_visible = {
				has_country_flag = gui_alert_visible_2
			}
		}
	}
}